import { DashboardLayout } from '@/components'
import { ReactElement } from 'react'

const Settings = () => {
  return <div>Settings 25633</div>
}

Settings.getLayout = (page: ReactElement) => <DashboardLayout>{page}</DashboardLayout>

export default Settings
